
# Phase 15C′ Acceptance Criteria

Must pass:

- All CLIs return normalized exit codes
- Deterministic JSON outputs across runs
- Strict artifact mode works (no legacy fallback)
- Severity ordering consistent across modules
- Taxonomy enforcement ON passes valid inputs
