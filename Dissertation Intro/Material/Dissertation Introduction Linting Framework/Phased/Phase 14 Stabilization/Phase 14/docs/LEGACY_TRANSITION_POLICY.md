# Legacy Transition Policy

## Phase 14A
- legacy flat JSON artifacts are still accepted
- all newly written artifacts must use the envelope format
- legacy input should trigger a warning

## Phase 14B
- legacy input is rejected by default
- legacy input may be accepted only via an explicit override flag
