I need to create a bullet list of points to cover in a brief into presentation of what LLMs are to an economist with no prior knowledge about LLM beyond basic use of free ChatGPT via a third party service. Below my initial thoughts. Help me develop this.

I guess I need to cover or touch

- AI vs LLM (AI as a more widespread generic among non-AI community, but primarily referring to LLM chat bots)
- LLM models
    - General access models (available via various access modes, limited set)
    - Specialized models - extended set, usually require API access and/or an model hosting service (when actual models are provided, rather then access to running models)
- General purpose vs specialized models
- LLM access
    - official chat bot (full feature set for a given tier)
    - third party chat bots (limited feature sets, probably no tools beyond submission of a prompt)
    - API for programmatic access and LLM-powered apps.
- token
    - input/output limits, about 2 tokens per English word
- context memory
    - measured in tokens
    - hierarchy
        - query only (e.g., likely, when using a third-party free service)
        - session (both chat bots and via API)
        - project level
        - account/global
- What are agents?